services:
  - type: web
    name: PrototipoPoliticore
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: /opt/render/project/src/.venv/bin/gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0