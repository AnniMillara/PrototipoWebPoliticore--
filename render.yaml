services:
  - type: web
    name: PrototipoPoliticore
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: /opt/render/project/src/.venv/bin/python -m waitress --host 0.0.0.0 --port $PORT app:app